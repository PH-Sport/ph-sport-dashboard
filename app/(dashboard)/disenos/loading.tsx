import { DesignsSkeleton } from '@/components/skeletons/designs-skeleton';

/** Fallback de navegación de /disenos (clic instantáneo). */
export default function Loading() {
  return <DesignsSkeleton />;
}
