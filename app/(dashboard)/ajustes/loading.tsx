import { SettingsSkeleton } from '@/components/skeletons/settings-skeleton';

/** Fallback de navegación de /ajustes y subrutas (clic instantáneo). */
export default function Loading() {
  return <SettingsSkeleton />;
}
